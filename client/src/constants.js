// This tool is built for a single client (Joseph Kim); the NMLS number is fixed rather
// than user-editable so it can never be changed to the wrong license number.
export const NMLS_NUMBER = '684894'
